#!/usr/bin/env python

import nltk

class DwordTokenizer

	def __init__(self, text="EMPTY", corpus='corpus', settings = {}):
		
		self.text = text
		self.corpus = corpus
		self.settings = settings
    	self._word_tokenize = word_tokenizer.tokenize
    	self._sent_tokenize = sent_tokenizer.tokenize

	def 
		

